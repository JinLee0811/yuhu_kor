# 유후 일일 보고 — 2026-04-13

---

## [개발자] 2026-04-13

**완료**
- mock 직접 사용 전면 제거 — 모든 UI가 Supabase API 호출로 동작
  - `BoardPostDetail.tsx` — 게시판 댓글/좋아요/조회수 API 연동
  - `reviews/[id]/page.tsx` — 리뷰 상세 + 댓글 API 연동
  - `EntityCard.tsx` — AI 요약 mock 기능 제거
  - `reviews/write/page.tsx` — mock 의존 유틸 → `lib/utils/findSchoolId.ts` 분리
- board 댓글 API 라우트 4개 신규 생성
  - GET/POST `/api/v1/board/[id]/comments` (인증 approved + admin만)
  - POST `/api/v1/board/[id]/like`, `/view`, `/comments/[commentId]/like`
- 리뷰 댓글 API 라우트 2개 신규 생성
  - GET/POST `/api/v1/reviews/[id]/comments`
  - POST `/api/v1/reviews/comments/[commentId]/like`
- board/reviews repository 댓글 함수 추가
- Supabase `increment_board_comment_count` RPC 마이그레이션 (대표님 직접 실행)
- PostHog는 MAU 100명 이상 시점으로 보류 기록
- agents/DEVELOPER.md 상태 갱신
- GitHub main 브랜치 푸시 완료

**이슈 / 미완료**
- 없음

**다음 우선순위**
1. Vercel 배포 (대표님 준비되면) — 환경변수 세팅 + og-image.png 제작
2. Rate Limiting (MAU 500+ 이후)
3. Kakao OAuth 서비스 심사
